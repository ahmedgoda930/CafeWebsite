@model IEnumerable<CafeWebsite.Models.MenuCat>

@{
    ViewBag.Title = "Index";
}

<div class="card">
    <div class="header">
        <h2 class="title">Categories'List</h2>
        <h3 class="category">@Html.ActionLink("Insert New Menu's Category", "Create")</h3>
    </div>
    <div class="content table-responsive table-full-width">

        <table class="table table-bordered  table-hover table-striped">
            <thead>
                <tr>
                    <th style="width:250px">Name</th>
                    <th class="op-width">Operations</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
            {
                    <tr>
                        <td>@item.Name</td>
                        <td>
                            <a href="/MenuCat/Edit/@item.ID" class="btn btn-primary btn-tbl"><i class="fa fa-edit"></i></a>
                            <a href="/MenuCat/Delete/@item.ID" class="btn btn-danger btn-tbl"><i class="fa fa-remove"></i></a>
                            @*<a href="/Studio/Details/@item.ID" class="btn btn-info btn-tbl"><i class="fa fa-file"></i></a>*@
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <div class="card-footor">
            <label>Count : </label>
            <label class="count">@Model.Count()</label>
        </div>
    </div>
</div>